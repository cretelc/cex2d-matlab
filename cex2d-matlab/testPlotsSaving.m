function testPlotsSaving(filename, results_folder)
    arguments
       filename string;
       results_folder string;
    end
    mkdir results_Folder newFolder
    mkdir(results_folder, "Plots")
end